// Add Javascript functionality here
// - Update musical notation based on selected chords (consider using a library like VexFlow)
// - Implement play button functionality (consider using a library like Tone.js)

const chordSelects = document.querySelectorAll("select");
const staff = document.querySelector(".staff");

chordSelects.forEach((select) => {
  // select.addEventListener("change", updateStaff);
});
